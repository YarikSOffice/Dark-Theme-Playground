<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="example.com.darkthemeplayground">

    <application
            android:name=".DarkThemeApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.DayNight"
            tools:ignore="GoogleAppIndexingWarning">

        <activity
                android:theme="@style/AppTheme.Light.IgnoreOverrideForceDarkOption"
                android:name=".IgnoreOverrideForceDarkOptionActivity"/>
        <activity
                android:name=".ForceDarkActivity"
                android:theme="@style/AppTheme.Light"/>

        <activity android:name=".AlwaysDarkActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>